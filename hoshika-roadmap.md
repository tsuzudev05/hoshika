# ホシカ

## プロダクト定義

**「欲しいものリスト × 予算管理アプリ」**

> 自由に使えるお金の中で、何を・いつ・どの順番で買うかを決めるアプリ

---

## 開発ロードマップ
> 2026年6月 → 11月リリース
>
> **真の目的**: DDDとクリーンアーキテクチャを体得する。
> 動くものを作ることより、**変更容易性を担保した頑強な設計**を習得することを最優先とする。

---

## マイルストーン

| 時期 | 目標 | 設計上の到達点 |
|------|------|----------------|
| 6月末 | ドメインモデル確定・環境構築 | ユビキタス言語・集約・境界の定義完了 |
| 8月末 | 動くMVP完成 | Clean Architectureの全レイヤー実装済み |
| 9月末 | ステージング公開 | 変更容易性の検証完了 |
| 11月末 | 🎉 本番リリース | 設計判断の言語化・アウトプット完了 |

---

## 設計原則（全フェーズを通じた指針）

```
依存の方向は常に内側へ

  Presentation
      ↓
  Application
      ↓
  Domain        ← ここにビジネスルールを閉じ込める
      ↑
  Infrastructure（DBやフレームワークはここ）
```

- **ドメイン層はRustの標準ライブラリのみ依存**。AxumもSQLxも知らない
- **リポジトリはtraitで定義**し、implはInfrastructure層に置く
- **ユースケースはドメインオブジェクトを操作する手順書**。HTTPの概念を持ち込まない
- 「このコードはどのレイヤーか？」を常に問いかけながら実装する

---

## Phase 01 · 6月　ドメインモデリング・設計

> **このフェーズで学ぶこと**: コードを書く前にドメインを理解する習慣。DDDの出発点は「言語の統一」

### ユビキタス言語の定義
- 「欲しいもの」をなんと呼ぶか決める（`WishItem`? `DesiredItem`?）
- 「衝動買い防止」はどういうドメインルールか言語化する
- 用語集（Glossary）をREADMEに書く

### 境界づけられたコンテキストの識別
- **欲しいものコンテキスト** — アイテムの登録・管理・優先順位
- **予算管理コンテキスト** — 月次予算・残高・購入記録
- **衝動買い防止コンテキスト** — 「本当に欲しいか？」チェックフロー
- コンテキスト間の関係（Context Map）をスケッチする

### ドメインモデル設計
- **エンティティの識別** — 同一性（ID）で追跡されるもの（例: WishItem, Budget）
- **値オブジェクトの識別** — 値で比較されるもの（例: Price, Category, WaitingPeriod）
- **集約の設計** — トランザクション一貫性の境界を決める（例: WishItemとそのCheckフローは同じ集約か？）
- **ドメインイベントの洗い出し** — 何が起きたかを記録（例: `ItemAdded`, `BudgetExceeded`, `ImpulseBuyPrevented`）

### DB設計（ドメインモデルから導出する）
- ドメインモデルが先、テーブル設計はその写像
- PostgreSQLスキーマ設計（`wish_items`, `budgets`, `categories`）
- 注意: 「テーブルの都合でエンティティを歪めない」

### 環境構築・リポジトリ整備
- Rust / React+TS / PostgreSQL / Fly.io の構成を DevContainer に整備
- プロジェクト構造をレイヤーで切る（後述のディレクトリ構成を参照）
- GitHub repo作成・CI/CD（GitHub Actions）基礎設定

---

## Phase 02 · 7月　ドメイン層・アプリケーション層（Rust）

> **このフェーズで学ぶこと**: フレームワークに依存しないビジネスロジックの書き方。テストのしやすさが設計の良さを測るバロメーター

### ディレクトリ構成（Clean Architecture）

```
src/
├── domain/
│   ├── entities/         # エンティティ・集約ルート
│   ├── value_objects/    # 値オブジェクト
│   ├── repositories/     # traitのみ（インターフェース定義）
│   ├── services/         # ドメインサービス（複数集約をまたぐロジック）
│   └── events/           # ドメインイベント
├── application/
│   └── use_cases/        # ユースケース（アプリケーションサービス）
├── infrastructure/
│   ├── db/               # SQLxによるリポジトリimpl
│   └── auth/             # JWT実装
└── presentation/
    └── handlers/         # Axumハンドラー（薄いラッパー）
```

### ドメイン層の実装
- `WishItem`エンティティ（IDによる同一性、不変条件をメソッドで保護）
- `Price`・`Category`・`WaitingPeriod`値オブジェクト（不正な値はコンパイル時・生成時に弾く）
- `WishItemRepository` trait（`find_by_id`, `save`, `delete` — DBを知らない）
- `BudgetService` ドメインサービス（予算超過チェックは複数集約をまたぐため）

### アプリケーション層の実装
- `AddWishItemUseCase` — バリデーション → リポジトリ保存 → イベント発行
- `CheckImpulseBuyUseCase` — 待機期間チェック → 本当に欲しいか判定ロジック
- `GetBudgetStatusUseCase` — 残高計算
- ユースケースはHTTPを知らない。引数はDTO、戻り値もDTO

### テスト（設計の良さを検証する）
- ドメイン層: `cargo test` だけで全テスト通る（DBもAxumも不要）
- アプリケーション層: リポジトリをモックして振る舞いをテスト
- ✅ チェックポイント: 「HTTPリクエストなしにビジネスロジックをテストできるか？」

---

## Phase 03 · 8月前半　インフラ層・プレゼンテーション層（Rust）

> **このフェーズで学ぶこと**: 依存逆転の原則の実感。traitのimplを差し替えるだけで外部依存を切り替えられる

### インフラ層の実装
- `PostgresWishItemRepository` — `WishItemRepository` traitのimpl（SQLxで実装）
- `JwtAuthService` — 認証の実装はここ
- DIコンテナ的な組み立て（Rustではstate管理やtrait objectで）

### プレゼンテーション層の実装
- Axumハンドラーは「リクエストのパース → ユースケース呼び出し → レスポンス変換」のみ
- ビジネスロジックがハンドラーに漏れていたら設計ミスのサイン

### 🔬 変更容易性の検証（重要）
- **リポジトリをSQLiteに差し替えてみる** — `SqliteWishItemRepository`を実装してテストが通るか確認
- これが通れば「データ永続化の詳細をドメインが知らない」設計が証明される
- 通らない場合は設計に漏れがある → 修正してレイヤー境界を正す

---

## Phase 04 · 8月後半　フロントエンド実装（React + TypeScript）

> **このフェーズで学ぶこと**: フロントエンドでも関心の分離を意識する

- Vite + React + TypeScript セットアップ
- **API層の分離** — APIコール関数を `src/api/` に閉じ込め、コンポーネントが直接fetchしない
- UIコンポーネント実装（欲しいものカード・予算メーター・カテゴリフィルター）
- 衝動買い防止フロー — 「本当に欲しいか？」チェックUI
- TanStack Queryで状態管理（サーバー状態とUIの状態を分ける）
- レスポンシブ対応（スマホファースト）

---

## Phase 05 · 9月　品質・インフラ

> **このフェーズで学ぶこと**: 設計の良さはテストカバレッジと変更のしやすさで測る

- **Fly.ioデプロイ** — ステージング環境・自動デプロイ
- **Sentry導入** — エラートラッキング（アプリ層とインフラ層でのエラー分類も意識）
- **E2Eテスト** — Playwrightで主要フロー
- **パフォーマンス計測** — Lighthouse・DBクエリ最適化
- **セキュリティ確認** — CORS・SQLインジェクション・認証周りの確認
- ✅ チェックポイント: 「新機能を追加するとき、どのレイヤーを触るか迷わないか？」

---

## Phase 06 · 10月　仕上げ・言語化

> **このフェーズで学ぶこと**: 設計判断を言語化することで理解が定着する

- **UI磨き込み** — 細部のUX改善・アニメーション・ローディング状態
- **PWA対応** — マニフェスト・Service Worker
- **README整備** — なぜこの設計にしたか・トレードオフ・アーキテクチャ図を書く
- **Zenn記事執筆** — 「RustでDDD+クリーンアーキテクチャを実践した」知見を記事化
  - どこで悩んだか・失敗した設計・直した理由を正直に書く
- **ユーザーテスト** — 身近な人に使ってもらいフィードバック収集

---

## Phase 07 · 11月　リリース

- **本番環境デプロイ** — Fly.io本番環境・ドメイン設定
- **バグ修正・安定化** — Sentry活用
- **career-log記録** — 設計判断・苦労した点・学びを career-log に記録
  - 「DDDをやってみてわかったこと」「クリーンアーキテクチャの実感値」
- **ポートフォリオ掲載** — 転職活動用のプロジェクト説明文（アーキテクチャの工夫を中心に）
- **🎉 リリース完了** — ホシカ公開！

---

## 振り返りの問い（各フェーズ末に自問する）

1. **このビジネスルールをどのレイヤーに書いたか？　なぜ？**
2. **このコードを変更するとき、他のどのコードが影響を受けるか？**
3. **テストを書くのが難しい場所はどこか？　それは設計の問題では？**
4. **ドメインエキスパート（ユーザー）と話せる言葉でコードが書けているか？**

---

> ※ 現職リアーキテクチャ（7月リリース予定）と並行するため、6〜7月は負荷調整を意識する

---

## AIが書いたコードをレビューするために必要なスキル

> AI生成コードは「動く」ことが多いが、「正しい設計」かどうかは別問題。
> レビュアーとして問うべきは「なぜこう書いたか」を説明できるか、だ。

### 1. アーキテクチャ整合性の判断力
AIはリクエストされたファイルだけを見て書く。システム全体の文脈を知らない。
- このコードはどのレイヤーに属するか？　層をまたいでいないか？
- 既存のパターン（リポジトリの命名・エラー型・DTO設計）と統一されているか？
- 依存の方向が逆転していないか（ドメイン層がInfraを知っていないか）

### 2. ドメインロジックの正しさを問う力
AIはドメインを知らない。ビジネスルールが誤実装されていても構文エラーは出ない。
- 集約の不変条件（invariant）が守られているか？
- 値オブジェクトが不正な状態を許容していないか？
- ユビキタス言語と命名が一致しているか？

### 3. テストの品質を評価する力
AIは「動くテスト」を書くが「意味のあるテスト」を書くとは限らない。
- ハッピーパスしかテストしていないか？
- ドメインルールの境界値（例: 予算ちょうど・予算+1円）をテストしているか？
- モックが過剰で「テストが仕様を表していない」状態になっていないか？

### 4. エラーハンドリングの深さ
AIのエラー処理は浅くなりがち。
- エラー型は適切か？（`unwrap()`の放置・汎用的すぎる`500 Internal Server Error`）
- エラーがどのレイヤーで処理されるべきか意識されているか？
- ユーザーに見せるエラーと内部ログ用エラーが混在していないか？

### 5. セキュリティの目線
- SQLインジェクション・パラメータバインドが適切か
- 認証・認可チェックが漏れていないか（エンドポイント追加時に認証ミドルウェアを通しているか）
- センシティブな情報がログに出力されていないか

### 6. 変更容易性の維持
- このコードを追加することで、既存のレイヤー境界が崩れていないか？
- 「後で直せばいい」と思わせるような密結合が生まれていないか？
- テストなしで追加されたロジックがないか

### レビューの心構え
AIに「動くコードを出力させる」のは簡単になった。
差別化できるのは「**そのコードが変更に耐えられるか判断できる**」能力。
レビューは品質のゲートではなく、**設計の対話の場**として使う。

---

## DDD・クリーンアーキテクチャ 学習リソース

### 書籍（優先度順）

| 優先度 | 書籍 | 一言メモ |
|--------|------|----------|
| ⭐️⭐️⭐️ | **ドメイン駆動設計入門** 成瀬允宣（ボトムアップDDD） | 日本語・実践的・コード例豊富。最初の1冊 |
| ⭐️⭐️⭐️ | **Clean Architecture 達人に学ぶソフトウェアの構造と設計** Robert C. Martin | 設計原則（SOLID）からアーキテクチャへの接続が明快 |


### 動画・記事

| リソース | 内容 |
|----------|------|
| [DDD Europe（YouTube）](https://www.youtube.com/@ddd_eu) | DDDの第一人者たちの講演集。Vaughn Vernon・Eric Evansの講演あり |
| [Clean Coders（Uncle Bob）](https://www.youtube.com/results?search_query=uncle+bob+clean+architecture) | Clean Architectureの著者本人による解説 |
| [little-hands' tech blog（Zenn/Qiita）](https://little-hands.hatenablog.com/) | 日本語DDD実践ブログ。集約設計・リポジトリパターンの解説が詳しい |
| [NTT Tech Conference / JJUG登壇資料] | 日本語でのDDD実践事例が多数 |

### 学習の順番（推奨）

```
1. ドメイン駆動設計入門（成瀬）   → DDDの概念を日本語で掴む
        ↓
2. Clean Architecture（Martin）   → レイヤー設計の原則を理解する
        ↓
3. 本プロジェクトで実装           → 手を動かして「実感」に変える
        ↓
4. IDDD（Vernon）/ 青本（Evans）  → 詰まったときの辞書として読む
```

> 書籍を読んでから実装するより、**実装で詰まってから本を開く**方が理解が深まる。
> このプロジェクト自体を「本の内容を検証する場」として使うこと。
